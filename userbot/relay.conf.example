# 이 기계에 telethon 발신기가 없을 때, 발신기가 있는 기계에 ssh 로 위임한다.
# 사본 자리: ~/.claude/userbot/relay.conf  (권한 600)
#
# 비밀번호 없이 붙어야 한다 — 세션은 사람이 없는 상태로 돌기 때문에 암호를 물으면
# 거기서 멈춘다. ssh 키를 먼저 넣어 둔다.
USERBOT_HOST=user@desktop
